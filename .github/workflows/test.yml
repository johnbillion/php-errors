name: Test
on:
  push:
    branches:
      - 'develop'
      - 'trunk'
  pull_request:
    branches:
      - '**'

jobs:
  build:
    strategy:
      matrix:
        php: ['8.1', '8.0', '7.4', '7.3', '7.2', '7.1', '7.0', '5.6']
      fail-fast: false
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-18.04
    timeout-minutes: 1
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Install PHP
      uses: shivammathur/setup-php@2.16.0
      with:
        php-version: ${{ matrix.php }}
        coverage: none
        tools: none
        ini-values: display_errors=Off, display_startup_errors=Off, error_reporting=E_ALL, zend.assertions=1, assert.exception=1, memory_limit=4M, max_execution_time=9
      env:
        fail-fast: true

    - name: Debugging
      run: |
        php --version
        php -m

    - name: Run test
      run: ./test.sh
